import React from 'react'

const QuestionModal = ({ open, close, question }) => (
  <div style={{ backgroundColor: 'green', borderRadius: 5 }}>
    AppCrashModal
    <button title="close" onClick={null} />
  </div>
)

export default QuestionModal
